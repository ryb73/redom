module Window = {
    include Window;
};

module Document = {
    include Document;
};

module Location = {
    include Location;
};

module Element = {
    include Element;
};

module IFrame = {
    include IFrame;
};
